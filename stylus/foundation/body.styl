/* Base dark theme
  mixin pulled from ./abstracts/_colors.styl */

body, html, :root {
  background-color fogra39
  color text-primary
}

* {
  box-sizing: border-box;
}

main {
  padding 1rem
  &:first-child {
    margin-top 0
  }
}
